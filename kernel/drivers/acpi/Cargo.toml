[package]
name = "mars_acpi_driver"
version = "0.0.1"
edition = "2024"

[lib]
name = "mars_acpi_driver"

[dependencies]
klib.workspace = true
log.workspace = true
creusot-std.workspace = true

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(creusot)'] }
